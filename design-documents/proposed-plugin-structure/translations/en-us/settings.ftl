# Contains translations for plugin settings
# Keys are referenced in plugin.kdl

enable_foobar = Enable Foobar
  .description = Enable the foobar service. Requires foobar.

provider-search-namespace = Provider Search Namespace
  .desc = Search namespace for the launcher provider.

number = Number Input
  .description = A number input. Doesn't really do anything.
